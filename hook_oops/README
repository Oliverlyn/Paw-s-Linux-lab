作用:
帮助调试内核模块的内核模块。

背景:
最早写这个模块是因为oops的时候，内核日志根本来不及写入文件就CPU就全stop了。
为了能看到完整的日志，把 panic 函数HOOK了，等个几秒再去死，这样能看到完整的日志了。
接着就各种奇葩的想法往这个模块里面加：UDP发送内核日志、检查链表、打印slab申请释放等。
最后实验室调试其他模块前都先把这个模块加载上。

原理：
http://blog.csdn.net/lucien_cc/article/details/7544834
这个文章提到的hook方法
和
将被HOOK函数头5字节改成jump

