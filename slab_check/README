作用:
内核slab链表遍历检查。

背景:
读研期间接手学长写的内核模块代码，出现随机oops，找到原因是使用slab_cache时越界，把内核其他slab_cache结构破坏。
但是没法定位到具体那块代码越界，就写了这个slab链表检查函数。
这个函数放到内核模块中，在几个可疑函数前和后分别检查slab链表是否完好，来确定哪个函数越的界。

原理：
遍历内核的cache_chain，检查每一个kmem_cache下挂的每条链表是否还完好。不完好就打印消息。
内核双向链表完好必须：1、首尾相接；2、每个list_head都必须是pos->next->prev == pos的。

